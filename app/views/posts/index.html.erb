<h1>Latest Posts</h1>

<section>
  <% @posts.each do |post| %>
    <article>
      <h3>
        <%= link_to post.title, post_path(post) %>
      </h3>
      <h5>
        <%= User.find(post.user_id).username %>
      </h5>
      <%= post.text %>
      <p class="date">
        <%= post.created_at %>
      </p>
    </article>
  <% end %>


</section>